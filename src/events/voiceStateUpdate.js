// const Discord = require("discord.js");
// const db = require("quick.db");
// var temporary = []

 module.exports = async (client, message) => { 

// try {
   
//    const queue = client.queue.get(message.guild.id)
//    const ch = queue.voiceChannel.id

//      // if(newMember.voiceChannelID == ch){
//      //     newMember.guild.createChannel(``)
//      //         .then(async channel => {
//      //             await newMember.setVoiceChannel(channel.id)
//      //             //console.log(`[TemporaryVoiceChannel] ${newMember.user.username} Berhasil Membuat Room`)
//      //         })
//      // }
//      temporary.push({ newID: ch, guild: message.guild })

//      if(temporary.length >= 0) for(let i = 0; i < temporary.length; i++) {   
//          let cha = message.guild.channels.cache.find(che => che.id === ch)
//          if(cha.members.size <= 0){
//          message.channel.send('Bot keluar')
//          return queue.delete(message.guild.id)
//          queue.voiceChannel.leave()         }
//      }
     
//    } catch (e) {
//      console.log(e)
//    }
}